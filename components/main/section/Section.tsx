import React from "react";
import styles from "../../../styles/main/section/_section.module.scss";

const Section = () => {
  return <section className={styles.sectionWrapper}>Section</section>;
};

export default Section;
